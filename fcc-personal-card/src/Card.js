import CardHeader from "./CardHeader";
import CardMain from "./CardMain";
function Card() {
  return (
    <div className="card">
      <CardHeader />
      <CardMain />
    </div>
  );
}

export default Card;
